name: Test custom comment body
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          repo-token: fake-token-for-testing
          comment-body: "This PR was included in release {{name}}. Thank you!"
          add-label: "false"
        continue-on-error: true
      - name: Assert action ran with custom comment body
        env:
          OUTCOME: ${{ steps.action.outcome }}
          PR_IDS: ${{ steps.action.outputs.pr-ids }}
        run: |
          echo "outcome: $OUTCOME"
          echo "pr-ids: $PR_IDS"
          # The action may fail due to fake token, but it should at least start
          if [ "$OUTCOME" = "success" ] || [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action ran to completion with custom comment body (outcome=$OUTCOME)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
